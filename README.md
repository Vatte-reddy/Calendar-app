first install dependices react+vite and styling using Tailwind css
 install tailwind Css
in src section create two Folders 
   ->components
   ->utilis

->In components create pages
   ->Hero_image page & Hero_name
   ->Notes panel
   ->Months section
   ->

->In utilis create user/custom hooks
  ->useNotes
  ->useDateRange
  ->useHero/usePlayer

  Reason behind custom hooks to make code clean and neat and use code in global level code

  